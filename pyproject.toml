[project]
name = "git"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "ipykernel>=7.3.0",
    "numpy>=2.5.3",
]

[dependency-groups]
dev = [
    "ruff>=0.16.8",
]
